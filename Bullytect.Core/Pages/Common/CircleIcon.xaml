<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
        xmlns="http://xamarin.com/schemas/2014/forms" 
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
        x:Class="Bullytect.Core.Pages.Common.CircleIcon"
        xmlns:artina="clr-namespace:UXDivers.Artina.Shared;assembly=UXDivers.Artina.Shared"
        xmlns:helper="clr-namespace:Bullytect.Core.Helpers;assembly=Bullytect.Core"
        x:Name="Root">
	<ContentView.Content>

        <Grid>
            <Label 
                Text="{ x:Static helper:GrialShapesFont.Circle }"
                IsVisible="{ Binding ShowBackgroundColor }" 
                Style="{ StaticResource FontIconBase }" 
                HorizontalTextAlignment="Center"
                VerticalTextAlignment="Center"
                Opacity="1"
                FontSize="60" 
                TextColor="{ DynamicResource OkColor }"
            />

            <Label 
                Text="{ x:Static helper:GrialShapesFont.ShoppingCart }"
                FontSize="25" 
                Style="{ StaticResource FontIcon }"
                HorizontalTextAlignment="Center"
                VerticalTextAlignment="Center"
                TextColor="{ DynamicResource InverseTextColor }"
            />
        </Grid>
        
	</ContentView.Content>
</ContentView>
